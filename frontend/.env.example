# Optional: for map picker and place search (India). If unset, manual lat/lng inputs are shown.
# 1. Create key: https://console.cloud.google.com/apis/credentials
# 2. Enable "Maps JavaScript API" and "Places API" (Library or APIs & Services) to avoid ApiNotActivatedMapError
VITE_GOOGLE_MAPS_API_KEY=
